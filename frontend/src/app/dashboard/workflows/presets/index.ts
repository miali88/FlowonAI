import { Presets } from '../types';
import { lawFirmPreset } from './law-firm';

export const presets: Presets = {
  "Law Firm Call Handling": lawFirmPreset,
  // Add more presets here
};

export { lawFirmPreset }; 