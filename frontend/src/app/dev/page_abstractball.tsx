'use client'

import React from 'react';
import AbstractBall from './abstract-ball';

const OrbPage: React.FC = () => {
  return (
    <div style={{ width: '100%', height: '100%' }}>
      <AbstractBall />
    </div>
  );
};

export default OrbPage;